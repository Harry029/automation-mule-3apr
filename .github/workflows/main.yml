name: Anypoint CLI Automation

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  cli:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v3
      
      - name: Install Anypoint CLI
        run: npm install -g anypoint-cli-v4
        
      - name: Config Organization
        run: >
          anypoint-cli-v4 conf organization b3875455-785b-461e-8239-06746330e0c6
      - name: Config Client Id
        run: >
          anypoint-cli-v4 conf client_id 8282233b5ab24364a05aa19000c5419e
      - name: Config Client Secret
        run: >
          anypoint-cli-v4 conf client_secret 29F4f3A45ed745CAB2229fc5449b11FA
      - name: List All Mule Apps
        run: >
          anypoint-cli-v4 runtime-mgr:application:list --environment Sandbox
